id: 
migration_tags: null
migration_group: null
label:
source:
  migration_name:
  plugin:
  path:
  base_query:
  attributes:

  xpath:

  keys:

process:
  type:
    plugin: default_value
    default_value: 

destination:
  plugin: 
dependencies:
  module:
migration_dependencies: {  }